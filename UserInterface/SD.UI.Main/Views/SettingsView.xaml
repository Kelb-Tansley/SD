<UserControl
    x:Class="SD.UI.Main.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="clr-namespace:SD.UI.Constants;assembly=SD.UI"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TabControl
                MinWidth="600"
                MinHeight="500"
                materialDesign:ColorZoneAssist.Mode="PrimaryDark"
                Style="{StaticResource MaterialDesignFilledTabControl}">
                <TabItem Header="Beam Property Settings">
                    <ContentControl prism:RegionManager.RegionName="{x:Static ui:RegionNames.BeamPropertiesRegion}" />
                </TabItem>
                <TabItem Header="Design Settings">
                    <ContentControl prism:RegionManager.RegionName="{x:Static ui:RegionNames.DesignSettingsRegion}" />
                </TabItem>
            </TabControl>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
              <Button
                  Margin="4,4,24,16"
                  Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                  Content="CLOSE"
                  IsDefault="False"
                  Style="{StaticResource MaterialDesignFlatButton}">
                  <i:Interaction.Triggers>
                      <i:EventTrigger EventName="Click">
                          <i:InvokeCommandAction Command="{Binding SaveSettingsCommand}" />
                      </i:EventTrigger>
                  </i:Interaction.Triggers>
              </Button>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
