<UserControl
    x:Class="SD.UI.UltimateLimitState.Views.CombinationsTableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:SD.UI.UltimateLimitState.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:CombinationsTableViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/SD.UI;component/Styles/Global.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DataGrid
        Margin="-8,0,-8,0"
        AutoGenerateColumns="False"
        CanUserAddRows="False"
        ItemsSource="{Binding LoadCaseCombinations}"
        SelectedItem="{Binding SelectedLoadCaseCombination, Mode=TwoWay}"
        SelectionMode="Single">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseLeftButtonUp">
                <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <DataGrid.Columns>
            <DataGridCheckBoxColumn Binding="{Binding Include}" IsReadOnly="True">
                <DataGridCheckBoxColumn.Header>
                    <Border Background="Transparent">
                        <CheckBox
                            d:DataContext="{d:DesignInstance Type=viewmodels:CombinationsTableViewModel}"
                            Command="{Binding CheckAllCommand}"
                            DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}"
                            IsChecked="{Binding AreAllSelected}" />
                    </Border>
                </DataGridCheckBoxColumn.Header>
                <DataGridCheckBoxColumn.HeaderStyle>
                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                    </Style>
                </DataGridCheckBoxColumn.HeaderStyle>
            </DataGridCheckBoxColumn>
            <DataGridTextColumn
                Width="58"
                Binding="{Binding Number}"
                Header="LCC"
                IsReadOnly="True" />
            <DataGridTextColumn
                Binding="{Binding Name}"
                Header="LCC Name"
                IsReadOnly="True" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
