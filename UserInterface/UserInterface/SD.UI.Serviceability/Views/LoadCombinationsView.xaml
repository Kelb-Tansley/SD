<UserControl
    x:Class="SD.UI.Serviceability.Views.LoadCombinationsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:SD.UI.Serviceability.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:LoadCombinationsViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/SD.UI;component/Styles/Global.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <GroupBox Margin="8,0,0,0" Header="Deflection Limit Options">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Margin="8" Text="Deflection Axis: " />
                    <ComboBox
                        Grid.Column="1"
                        Width="80"
                        Margin="8,0"
                        HorizontalAlignment="Left"
                        ItemsSource="{Binding DeflectionAxisViewModel.Options}"
                        SelectedItem="{Binding DeflectionAxisViewModel.Selected}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding AutoCalculateChangedCommand}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ComboBox>

                    <TextBlock
                        Grid.Row="1"
                        Margin="8"
                        Text="Deflection Method:" />
                    <ComboBox
                        Grid.Row="1"
                        Grid.Column="1"
                        Width="80"
                        Margin="8,0"
                        HorizontalAlignment="Left"
                        ItemsSource="{Binding DeflectionMethodViewModel.Options}"
                        SelectedItem="{Binding DeflectionMethodViewModel.Selected}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding AutoCalculateChangedCommand}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ComboBox>

                    <TextBox
                        Grid.Row="2"
                        Margin="8"
                        VerticalAlignment="Center"
                        materialDesign:HintAssist.Hint="Min span to deflection ratio"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding MinSpanDeflectionRatio}" />

                    <Button
                        Grid.Row="3"
                        Grid.Column="1"
                        Width="120"
                        HorizontalAlignment="Right"
                        Command="{Binding CalculateCommand}"
                        Content="CALCULATE"
                        IsEnabled="{Binding CanCalculate}"
                        Style="{StaticResource StandardButton}" />

                    <CheckBox
                        Grid.Row="3"
                        Margin="8"
                        Command="{Binding AutoCalculateChangedCommand}"
                        Content="Auto Calculate"
                        IsChecked="{Binding AutoCalculate}"
                        Style="{StaticResource DisplayCheckbox}" />
                </Grid>
            </GroupBox>

            <DataGrid
                Grid.Row="1"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                ItemsSource="{Binding LoadCaseCombinations}"
                SelectedItem="{Binding SelectedLoadCaseCombination}"
                SelectionMode="Single">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding Include}" IsReadOnly="True">
                        <DataGridCheckBoxColumn.Header>
                            <Border Background="Transparent">
                                <CheckBox
                                    Command="{Binding CheckAllCommand}"
                                    DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}"
                                    IsChecked="{Binding AreAllSelected}" />
                            </Border>
                        </DataGridCheckBoxColumn.Header>
                        <DataGridCheckBoxColumn.HeaderStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                            </Style>
                        </DataGridCheckBoxColumn.HeaderStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn
                        Binding="{Binding Number}"
                        Header="Number"
                        IsReadOnly="True" />
                    <DataGridTextColumn
                        Binding="{Binding Name}"
                        Header="Name"
                        IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid
            Panel.ZIndex="10000"
            Background="{StaticResource MaterialDesignLightBackground}"
            Opacity="0.6"
            Visibility="{Binding Path=IsThisProcessRunning, Converter={StaticResource BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
            <ProgressBar
                IsIndeterminate="True"
                Style="{StaticResource MaterialDesignCircularProgressBar}"
                Value="0" />
        </Grid>
    </Grid>
</UserControl>
