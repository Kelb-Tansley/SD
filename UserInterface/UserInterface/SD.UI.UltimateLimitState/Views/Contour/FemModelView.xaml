<UserControl
    x:Class="SD.UI.UltimateLimitState.Views.FemModelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:SD.UI.UltimateLimitState.ViewModels"
    xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
    d:DataContext="{d:DesignInstance Type=viewmodels:FemModelViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="LightGray"
    mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
        <i:EventTrigger EventName="Unloaded">
            <i:InvokeCommandAction Command="{Binding UnloadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid Visibility="{Binding ViewManagementModel.IsDrawerOpen, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
        <WindowsFormsHost>
            <wf:PictureBox x:Name="FemModelPanel" SizeChanged="FemModelPanel_SizeChanged" />
        </WindowsFormsHost>
    </Grid>
</UserControl>
