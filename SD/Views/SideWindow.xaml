<Window
    x:Class="SD.Views.SideWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behavior="clr-namespace:SD.UI.Behaviors;assembly=SD.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:ui="clr-namespace:SD.UI.Constants;assembly=SD.UI"
    xmlns:viewmodels="clr-namespace:SD.ViewModels"
    Title="Aurestruct"
    behavior:MainWindowBehavior.WindowResizer="MainWindowResizer"
    d:DataContext="{d:DesignInstance Type=viewmodels:DesignViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Style="{StaticResource MainWindowStyle}"
    WindowState="Maximized"
    mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding ClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ContentControl prism:RegionManager.RegionName="{x:Static ui:RegionNames.SideWindowHeaderRegion}" />
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static ui:RegionNames.SideWindowDesignRegion}" />
    </Grid>
</Window>
